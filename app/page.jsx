'use client';
import{useEffect,useMemo,useState}from'react';import{createClient}from'@supabase/supabase-js';
const db=process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY?createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY):null;const games=['8-ball','9-ball','10-ball'],rules=['APA','BCA','Bar room rules'],durations=[['30 min',30],['1 hour',60],['2 hours',120],['4 hours',240],['All night',360]];const name=p=>p?.display_name||p?.username||'Pool player';const remaining=x=>{const n=Math.max(0,Math.ceil((new Date(x)-Date.now())/60000));return n<60?`${n} min left`:`${Math.ceil(n/60)} hr left`};
export default function App(){const[user,setUser]=useState(),[venues,setVenues]=useState([]),[live,setLive]=useState([]),[profile,setProfile]=useState(),[form,setForm]=useState({venue_id:'',game_type:'8-ball',rules_type:'APA',minutes:120,note:''}),[notice,setNotice]=useState(''),[busy,setBusy]=useState(false);const mine=useMemo(()=>live.find(x=>x.user_id===user?.id),[live,user]);useEffect(()=>{if(!db)return;db.auth.getUser().then(({data})=>{setUser(data.user);load(data.user?.id)})},[]);async function load(id=user?.id){if(!db)return;const[v,c]=await Promise.all([db.from('venues').select('*').order('city').order('name'),db.from('check_ins').select('*,profiles!check_ins_user_id_fkey(*),venues!check_ins_venue_id_fkey(*)').eq('status','active').gt('expires_at',new Date().toISOString()).order('created_at',{ascending:false})]);if(v.error)setNotice(v.error.message);else{setVenues(v.data||[]);setForm(o=>({...o,venue_id:o.venue_id||v.data?.[0]?.id||''}))}if(c.error)setNotice(c.error.message);else setLive(c.data||[]);if(id){const p=await db.from('profiles').select('*').eq('id',id).maybeSingle();if(p.error)setNotice(p.error.message);else setProfile(p.data)}}async function auth(e){e.preventDefault();const f=new FormData(e.currentTarget),x={email:f.get('email'),password:f.get('password')};let r=await db.auth.signInWithPassword(x);if(r.error)r=await db.auth.signUp(x);if(r.error)return setNotice(r.error.message);if(!r.data.user)return setNotice('Check your email to confirm this test account, then sign in.');setUser(r.data.user);load(r.data.user.id)}async function ensureProfile(){if(profile)return true;const username=`player${user.id.slice(0,6)}`;const r=await db.from('profiles').insert({id:user.id,username,display_name:username});if(r.error){setNotice(r.error.message);return false}setProfile({id:user.id,username,display_name:username});return true}async function goLive(){if(busy)return;setBusy(true);setNotice('');if(!(await ensureProfile()))return setBusy(false);if(mine){setNotice('You are already checked in. Check out before starting another session.');return setBusy(false)}const r=await db.from('check_ins').insert({user_id:user.id,venue_id:form.venue_id,game_type:form.game_type,rules_type:form.rules_type,note:form.note.trim()||null,status:'active',expires_at:new Date(Date.now()+form.minutes*60000).toISOString()});setBusy(false);if(r.error)setNotice(r.error.message);else{setNotice('You are live.');setForm(o=>({...o,note:''}));load()}}async function checkout(){if(!mine||busy)return;setBusy(true);const r=await db.from('check_ins').update({status:'ended',ended_at:new Date().toISOString()}).eq('id',mine.id).eq('user_id',user.id);setBusy(false);if(r.error)setNotice(r.error.message);else{setNotice('You are checked out.');load()}}if(!db)return <main><h1>Rack Up needs staging configuration.</h1></main>;if(!user)return <main className="auth"><section><b>△ Rack Up · STAGING</b><h1>Find a game tonight.</h1><form onSubmit={auth}><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" minLength="6" placeholder="Password" required/><button>Sign in or create test account</button></form>{notice&&<p className="notice">{notice}</p>}</section></main>;return <main><header><b>△ Rack Up <small>STAGING</small></b><button className="link" onClick={()=>db.auth.signOut().then(()=>{setUser();setProfile();setLive([])})}>Sign out</button></header><section className="hero"><p>GREATER BOSTON · {live.length} LIVE</p><h1>Find your next set.</h1></section>{notice&&<p className="notice">{notice}</p>}<section className="checkin"><p>YOUR AVAILABILITY</p><h2>{mine?`You are live at ${mine.venues?.name}.`:'Ready to play?'}</h2>{mine?<><p>{mine.game_type} · {mine.rules_type} · {remaining(mine.expires_at)}</p><button className="checkout" disabled={busy} onClick={checkout}>{busy?'Working…':'Check out'}</button></>:<div className="form"><label>Venue<select value={form.venue_id} onChange={e=>setForm({...form,venue_id:e.target.value})}>{venues.map(x=><option value={x.id} key={x.id}>{x.name} — {x.city}</option>)}</select></label><Picker label="Game" items={games} value={form.game_type} choose={game_type=>setForm({...form,game_type})}/><Picker label="Rules" items={rules} value={form.rules_type} choose={rules_type=>setForm({...form,rules_type})}/><label>Staying for<select value={form.minutes} onChange={e=>setForm({...form,minutes:+e.target.value})}>{durations.map(([a,b])=><option value={b} key={b}>{a}</option>)}</select></label><label>Optional note<input maxLength="240" value={form.note} onChange={e=>setForm({...form,note:e.target.value)} placeholder="Looking for a race to 3."/></label><button disabled={busy||!form.venue_id} onClick={goLive}>{busy?'Going live…':'Go live'}</button></div>}</section><h2>Players checked in nearby</h2><div className="cards">{live.length?live.map(x=><article key={x.id}><b>{name(x.profiles)}</b><span>{remaining(x.expires_at)}</span><p>{x.venues?.name} · {x.venues?.city}</p><i>{x.game_type}</i> <i>{x.rules_type}</i>{x.note&&<p>“{x.note}”</p>}</article>):<p className="empty">No active players yet.</p>}</div><nav><button className="active">Home</button><button>Following</button><button>Messages</button><button>Profile</button></nav></main>}
function Picker({label,items,value,choose}){return <div className="picker"><b>{label}</b><div>{items.map(x=><button type="button" className={x===value?'selected':''} onClick={()=>choose(x)} key={x}>{x}</button>)}</div></div>}
